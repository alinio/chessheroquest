/**
 * Lichess Opening Explorer client — real move frequencies / win-rates / lines.
 * The PRIMARY opening database (CLAUDE.md DATA SOURCES). Free, no key, public.
 * Respect rate limits + attribution. This is part of the TRUTH layer: it returns
 * verified data only; it never invents moves or evaluations.
 *
 * Step 0 stub — implementation owned by the `chess-engine` agent.
 */
export {};
