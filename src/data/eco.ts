/**
 * ECO opening files (PGN, open source) loader — opening structure and names.
 * Bundled, no key. Part of the TRUTH layer (CLAUDE.md DATA SOURCES).
 *
 * Step 0 stub — implementation owned by the `chess-engine` agent.
 */
export {};
