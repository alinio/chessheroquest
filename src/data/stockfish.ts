/**
 * Stockfish (WASM) evaluation wrapper. Client-side, free, no server cost.
 * Runs in a Web Worker so evaluation never blocks the UI (ARCHITECTURE.md).
 * Part of the TRUTH layer: evaluations come from Stockfish only — the LLM never
 * evaluates a move (CLAUDE.md LAW #2).
 *
 * Step 0 stub — implementation owned by the `chess-engine` agent.
 */
export {};
